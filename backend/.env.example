# Copy to `.env` in this directory for local development.
# NEVER commit the real `.env` — it holds secrets.

DATABASE_URL=postgres://cat:cat@localhost:5432/cat?sslmode=disable
JWT_SECRET=dev-secret-trocar
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=admin123

# Optional (defaults shown)
# PORT=8080
# APP_ENV=development
